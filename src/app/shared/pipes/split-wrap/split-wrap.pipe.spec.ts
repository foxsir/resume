import { SplitWrapPipe } from './split-wrap.pipe';

describe('SplitWrapPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitWrapPipe();
    expect(pipe).toBeTruthy();
  });
});
